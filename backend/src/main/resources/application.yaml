spring:
  application:
    name: "reconciliation-transactions-financieres"
  batch:
    job:
      enabled: false
  mongodb:
    uri: "mongodb://user:password@localhost:27017/bank?authSource=admin"

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG